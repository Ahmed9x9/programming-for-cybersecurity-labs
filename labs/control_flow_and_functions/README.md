# Control flow and functions

This topic combines the conditions/functions report with the related control-flow scenarios. It
covers numbered attack output, password classification, reversible XOR, Gregorian leap years, the
secret-number loop, and pyramid height.

Password strength is defined consistently: all five checks is **strong**, three or four is
**moderate**, and fewer than three is **weak**.

```bash
python -m labs.control_flow_and_functions.solutions --password 'GoodJob1!'
```
