# Collections

The solutions demonstrate list operations on common service ports and dictionary operations on a
small, documentation-only ARP table. The spoofing exercise changes an in-memory dictionary entry; it
does not send ARP traffic.

```bash
python -m labs.collections.solutions
```
