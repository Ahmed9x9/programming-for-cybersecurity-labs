# OOP fundamentals

The bank-account solution validates deposits and withdrawals and stores currency with `Decimal`.
The prime-number solution separates factor calculation from formatted output and demonstrates
inheritance through `DetailedPrimeNumberChecker`.

```bash
python -m labs.oop_fundamentals.solutions --number 28
```
